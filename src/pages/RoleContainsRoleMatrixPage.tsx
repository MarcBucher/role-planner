import { PageContainer } from '../components/layout/PageContainer';
import { RoleContainsRoleMatrix } from '../components/matrices/RoleContainsRoleMatrix';

export function RoleContainsRoleMatrixPage() {
  return (
    <PageContainer title="Rolle × Rolle (Vererbung)">
      <RoleContainsRoleMatrix />
    </PageContainer>
  );
}
